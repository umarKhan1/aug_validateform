name: example
description: Example project for aug_validateform.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.6.0-0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  aug_validateform:
    path: ../packages/aug_validateform

dev_dependencies:
  flutter_test:
    sdk: flutter
  aug_validateform_generator:
    path: ../packages/aug_validateform_generator
  build_runner: ^2.4.8
  analyzer: ^6.4.1

flutter:
  uses-material-design: true
