# aug_validateform_annotations

Core annotations for `aug_validateform`. This is a pure Dart package extracted from the main `aug_validateform` package to enable high-performance code generation without Flutter SDK dependencies.

## Usage

This package is intended to be used by `aug_validateform_generator` and re-exported by `aug_validateform`. You typically won't need to import it directly.
